{
  "name": "@acme/logger",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsup",
    "watch": "tsup --watch",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "jest": {
    "preset": "@acme/jest-presets/jest/node"
  },
  "devDependencies": {
    "@acme/eslint-config": "workspace:*",
    "@acme/jest-presets": "workspace:*",
    "@acme/typescript-config": "workspace:*",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.25",
    "jest": "^29.7.0",
    "tsup": "^8.0.2",
    "typescript": "^5.4.2"
  }
}
